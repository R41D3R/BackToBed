package com.github.bakycoder.backtobed;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * The main class for the BackToBed mod.
 * Contains mod-specific constants and a logger instance.
 */
public class BackToBed {
	/**
	 * The mod ID, a unique identifier for the BackToBed mod.
	 * Used in various places for registration and identification.
	 */
	public static final String MOD_ID = "backtobed";

	/**
	 * The logger instance for the BackToBed mod.
	 * Used for logging messages during mod initialization and runtime.
	 */
	public static final Logger LOGGER = LoggerFactory.getLogger(BackToBed.class);
}
